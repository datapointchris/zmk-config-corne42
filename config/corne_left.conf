# Configuration specific to the Corne left half (central)
# The left half handles USB connections and pointing device processing

# Enable USB support for the central
CONFIG_ZMK_USB=y

# Enable smooth scrolling for pointing devices on central
CONFIG_ZMK_POINTING_SMOOTH_SCROLLING=y
