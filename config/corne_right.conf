# Configuration specific to the Corne right half (peripheral)
# The right half is a peripheral that only communicates with the central

# Explicitly disable central-only features for the peripheral
CONFIG_ZMK_USB=n